<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
             x:Class="ExamCalculator.UI.ExamDetail"
             Name="examDetail">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>


        <StackPanel Grid.Row="0" Classes="Toolbar">
            <TextBlock Text="{Binding Caption}"  />
            <TextBox Watermark="Nummer (1a, 2d, ...)" />
            <TextBox Watermark="Punkte" />
            <Button Command="{Binding CreateTask}">+Aufgabe</Button>
        </StackPanel>


        <DataGrid Grid.Row="1"
                  Items="{Binding ExamTasks}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Nummer" Width="*" Binding="{Binding Number}" />
                <DataGridTextColumn Header="Punkte" Width="*" Binding="{Binding MaximumPoints}" />
            </DataGrid.Columns>
        </DataGrid>


    </Grid>
</UserControl>